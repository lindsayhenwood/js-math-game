
var lifeBalance = 3;
var mathQuestion = Math.floor(Math.random() * 20);
var answer = true;
var playerOne = prompt("What is" + mathQuestion + "?");
var playerTwo = prompt("What is" + mathQuestion + "?");


var question = " ";
question += prompt("Player 1 what is: ");
var n1 = Math.round(Math.random() * 20);
var n2 = Math.round(Math.random() * 20);
question += n1


switch(n1)
	case '0.333333':
	{
		console.log("+");
	break;
	} 
	case '0.666666':
	{
		console.log("*");
		break;
	}
	default:
	{
		console.log("-");
	}

	//console.log(n1, 


var randomQuestion = function {
	return Math.random() + Math.random();
}

while (lifeBalance >= 0) {
	lifeBalance = -1
	if (mathQuestion == answer) {
		console.log("You are smart!");
	} else if {
		(mathQuestion != answer) {
			console.log("Not!");
		} else {
			lifeBalance -= 0;
		}
	}
}
 alert(lifeBalance);
}
